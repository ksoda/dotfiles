set -x EDITOR vim
alias e=$EDITOR
set -x LESS "-R"
set -x LESSOPEN "| /usr/share/source-highlight/src-hilite-lesspipe.sh %s"
set -x sudope_sequence \cs
# set -x PATH $PATH $GOPATH/bin
# set -x PATH $PATH $HOME/.volta/bin
# set -x PATH $PATH $HOME/.cargo/bin
# set -x PATH $PATH $HOME/.rbenv/bin

